#!/usr/bin/env ruby

# Release PhantomJS
release_phantomjs_path = File.expand_path("../release-phantomjs", __FILE__)
system release_phantomjs_path

$:.unshift File.expand_path("../../lib", __FILE__)
require "language_pack"

if pack = LanguagePack.detect(ARGV[0], ARGV[1])
  puts pack.release
end
